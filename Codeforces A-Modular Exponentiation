#include<iostream>
#include<cmath>
#include<cstdio>
using namespace std;

int main()
{
    int n, m, rem;
    cin >> n >> m;

    int x = pow(2, n);

    rem = m % x;

    cout << rem << endl;

    return 0;
}
