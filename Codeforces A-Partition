#include<iostream>
#include<cstdio>
using namespace std;

int main()
{
    int n, B = 0, C = 0, a;
    cin >> n;
    for(int i = 0; i < n ; i++)
    {
        cin >> a;
        if(a < 0) C += a;
        else B += a;
    }

    cout << B-C << endl;

    return 0;
}
