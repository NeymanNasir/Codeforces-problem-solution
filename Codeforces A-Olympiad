#include<iostream>
#include<cstdio>
using namespace std;

int ara[600];
int main()
{
    int n, a, cnt = 0;
    cin >> n;
    for(int i = 0; i < n; i++)
    {
        cin >> a;
        if(ara[a] == 0 && a != 0) ara[a] = 1, cnt++;
    }

    cout << cnt << endl;

    return 0;
}
