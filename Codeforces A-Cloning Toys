#include<iostream>
#include<cstdio>
using namespace std;

int main()
{
    int x, y;
    cin >> x >> y;

    if(y == 0 || (y == 1 && x > 0)) printf("No\n");
    else if((y == 1 && x == 0) || (x-(y-1)>= 0 && (x-(y-1))%2 == 0)) printf("Yes\n");
    else printf("No\n");

    return 0;
}
